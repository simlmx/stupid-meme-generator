# What we really need
#
# flask
# gunicorn

# With the versions and dependencies (output of `pip freeze`)
Click==7.0
Flask==1.0.2
gunicorn==19.9.0
itsdangerous==1.1.0
Jinja2==2.10.1
MarkupSafe==1.1.0
Werkzeug==0.15.5
