# What we really need
# flask
# gunicorn

# With the versions and dependencies (output of `pip freeze`)
Flask==0.10.1
gunicorn==19.9.0
itsdangerous==0.24
Jinja2==2.8
MarkupSafe==0.23
Werkzeug==0.10.4
